import { FilterMaterialPipe } from './filter-material.pipe';

describe('FilterMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
